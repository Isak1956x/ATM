<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
<link rel="stylesheet" href="/assets/css/lib/bootstrap/bootstrap.min.css">
</head>
<body class="bg-dark">
      {{#if error}}
   <div class="alert alert-danger alert-dismissible fade show text-center" role="alert">
  {{error}}
  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
         {{/if}}

  <div class="d-flex justify-content-center mt-5">
    <div class="card bg-secondary mt-5" style="width: 50rem;">
        <div class="card-header text-center bg-primary text-light"><h2>ATM</h2></div>
        <div class="card-body text-center  text-light">
            <h5 class="card-title">Welcome to the ATM service</h5>
            <p class="card-text">Please enter the amount you wish to withdraw:</p>
            <form action="/atm" method="post">
        <div class="mb-3">
      <input type="text" class="form-control {{#if error}}is-invalid{{/if}}" name="amount" placeholder="Enter amount" id="amount">
        </div>
     
         <div class="mb-3">
       <select name="mode" id="mode" class="form-select">
        <option value="1">Dispencion Eficiente</option>
        <option value="2">200 y 1000</option>
        <option value="3">100 y 500</option>
        </select>
        </div>
        <div class="d-flex justify-content-center mt-3">
            <button class="btn btn-warning" type="submit">Withdraw</button>
        </div>
    </form>
    </div>
</div>
    
  </div>
</body>
<script src="/assets/js/lib/bootstrap/bootstrap.min.js"></script>
</html>